<view class="container">
  <view class="section">
    <view wx:for="{{array}}" class="brand">
      <image class="product" src="{{item.image_src}}"></image>
      <view class="title"><text>{{item.name}}</text></view>
    </view>
  </view>
</view>